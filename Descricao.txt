Arquivos:
    Aresta -> Contém Classe Aresta, com atributos relevantes para as EdgeTables;
    EdgeTable -> Classe da EdgeTable, utilizada no algoritmo;
    main -> Arquivo principal, não desenvolver nele, nem colocar bugs;
    Makefile -> Arquivo com comandos para testagem do código;
    Ponto -> Classe Ponto;
    README -> Readme padrão;
    teste.py -> Lugar para desenvolver antes de ter certeza e passar para a main;


Ideia geral do algoritmo:
    1 - Pegamos os pontos pelos cliques na tela; (FEITO - Acho)
    2 - Dos pontos, nós pegamos as arestsa; (FEITO - Acho)
    3 - Inicializamos a ET e AET (são a mesma estrutura de dados) (Feito)
    4 - Preenchemos a ET de acordo com as arestas;
    5 - Fazemos o algoritmo de preenchimento em si;
    6 - Pensar na lógica do usuário, com botões, etc;

Conteúdo: 
    O algoritmo que devemos implementar está nos últimos slides da penúltima aula
